version: '3.8'
services:
  db:
    image: postgis/postgis:15-3.3
    environment:
      POSTGRES_USER: prithvi
      POSTGRES_PASSWORD: prithvi_password
      POSTGRES_DB: prithvi_watch
    ports:
      - "5432:5432"
    volumes:
      - postgis_data:/var/lib/postgresql/data
volumes:
  postgis_data:
